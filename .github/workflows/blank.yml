name: CI

on:
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Clean no projeto
      run: ./gradlew clean
    - name: Vê os tipos de tasks
      run: ./gradlew tasks,
    - name: Executa a task assemble
      run:  ./gradlew assemble
    - name: Finalizar
      run: echo Fim
      
